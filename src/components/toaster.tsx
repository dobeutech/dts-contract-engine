// Thin re-export so callers can `@/components/toaster` without knowing whether
// the implementation lives in `ui/sonner` (shadcn) or somewhere else.
export { Toaster } from "@/components/ui/sonner";
