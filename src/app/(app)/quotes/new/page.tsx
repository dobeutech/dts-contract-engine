import { PageStub } from "@/components/app/page-stub";

// Placeholder until Phase 4 lands the real quote builder.
export default function NewQuotePage() {
  return (
    <PageStub
      eyebrow="01 New quote"
      title="Build a quote"
      description="The quote builder is loading in. Pick an engagement type, scope it out, and watch the live summary update on the right."
    />
  );
}
