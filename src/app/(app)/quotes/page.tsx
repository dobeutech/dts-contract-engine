import { PageStub } from "@/components/app/page-stub";

export default function SavedQuotesPage() {
  return (
    <PageStub
      eyebrow="Library"
      title="Saved quotes"
      description="Saved quotes will list here once the quote builder writes its first draft."
    />
  );
}
