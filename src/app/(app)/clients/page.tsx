import { PageStub } from "@/components/app/page-stub";

export default function ClientsPage() {
  return (
    <PageStub
      eyebrow="Directory"
      title="Clients"
      description="Add a client and you can route quotes, contracts, and the consulting log through them."
    />
  );
}
