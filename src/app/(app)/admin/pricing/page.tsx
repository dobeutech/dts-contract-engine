import { PageStub } from "@/components/app/page-stub";

export default function PricingConfigPage() {
  return (
    <PageStub
      eyebrow="Configuration"
      title="Pricing &amp; defaults"
      description="Tier retainers, setup items, recurring add-ons, and overage rates. Versioned writes — old quotes stay locked to their original prices."
    />
  );
}
