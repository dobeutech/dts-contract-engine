interface:
  display_name: "Dts Contract Engine"
  short_description: "Repo-specific patterns and workflows for dts-contract-engine"
  default_prompt: "Use the dts-contract-engine repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true